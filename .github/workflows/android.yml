name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Install dependencies
        run: npm ci || npm install

      - name: Build React App
        run: npm run build

      - name: Initialize Capacitor Android
        run: |
          if [ -d "android" ]; then
            rm -rf android
          fi
          npx cap add android

      - name: Generate App Icons
        run: |
          if [ -f "shadowarts.png" ]; then
            mkdir -p assets
            cp shadowarts.png assets/logo.png
            npm install -D @capacitor/assets
            npx @capacitor/assets generate --android --iconBackgroundColor '#1e1b4b' --splashBackgroundColor '#1e1b4b'
          fi

      - name: Sync Capacitor
        run: |
          npx cap sync android
          find android -name "strings.xml" -exec sed -i 's/Költészet/ShadowArts/g' {} \;
          find android -name "strings.xml" -exec sed -i 's/Kolteszet/ShadowArts/g' {} \;
          find android -name "AndroidManifest.xml" -exec sed -i 's/hu.kolteszet.app/hu.shadowarts.app/g' {} \;
          find android -name "build.gradle" -exec sed -i 's/hu.kolteszet.app/hu.shadowarts.app/g' {} \;
          find android -name "build.gradle" -exec sed -i 's/applicationId "hu.kolteszet.app"/applicationId "hu.shadowarts.app"/g' {} \;

      - name: Configure Gradle Signing
        run: |
          if [ "${{ env.SIGNING_ENABLED }}" = "true" ]; then
            BUILD_GRADLE_PATH="android/app/build.gradle"
            sed -i '/android {/a \
                signingConfigs {\
                    release {\
                        storeFile file("keystore.jks")\
                        storePassword System.getenv("STORE_PASSWORD")\
                        keyAlias System.getenv("KEY_ALIAS")\
                        keyPassword System.getenv("KEY_PASSWORD")\
                    }\
                }' "$BUILD_GRADLE_PATH"
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/g' "$BUILD_GRADLE_PATH"
          fi

      - name: Make Gradle executable
        run: chmod +x android/gradlew

      - name: Configure Release Keystore
        run: |
          if [ "${{ github.event_name }}" = "push" ] && [ "${{ github.ref }}" = "refs/heads/main" ] && [ "${{ secrets.KEYSTORE_BASE64 }}" != "" ]; then
            echo "Configuring release keystore..."
            echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/keystore.jks
            echo "STORE_PASSWORD=${{ secrets.STORE_PASSWORD }}" >> $GITHUB_ENV
            echo "KEY_ALIAS=${{ secrets.KEY_ALIAS }}" >> $GITHUB_ENV
            echo "KEY_PASSWORD=${{ secrets.KEY_PASSWORD }}" >> $GITHUB_ENV
            export SIGNING_ENABLED=true
          else
            export SIGNING_ENABLED=false
          fi
          echo "SIGNING_ENABLED=$SIGNING_ENABLED" >> $GITHUB_ENV

      - name: Build Debug APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload Debug APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: shadowarts-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7

      - name: Build Release APK
        if: env.SIGNING_ENABLED == 'true'
        run: |
          cd android
          ./gradlew assembleRelease

      - name: Upload Release APK
        if: env.SIGNING_ENABLED == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: shadowarts-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Create GitHub Release
        if: env.SIGNING_ENABLED == 'true'
        uses: ncipollo/release-action@v1
        with:
          tag: v1.0.0
          name: ShadowArts v1.0.0
          body: |
            ## ShadowArts v1.0.0

            Első kiadás! 🎉

            ### Funkciók:
            - ✍️ Versek írása címmel, tartalommal és dátummal
            - 📚 Ciklusok kezelése versekkel
            - 💾 Exportálás JSON formátumban
            - 📥 Importálás JSON fájlból
            - 🔄 Automatikus ID-k az importált adatokhoz
          artifacts: android/app/build/outputs/apk/release/app-release.apk
          allowUpdates: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
